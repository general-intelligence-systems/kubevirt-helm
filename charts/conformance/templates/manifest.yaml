sonobuoy-config:
  driver: Job
  plugin-name: kubevirt-conformance
  result-format: junit
spec:
  command:
  - /usr/bin/conformance
  image: quay.io/kubevirt/conformance:v1.2.0
  imagePullPolicy: Always
  name: plugin
